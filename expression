#include<stdio.h>
main()
{
int num,i,sum=o;
printf("input any number:");
scanf("%d",&num);
printf("1+");
for(i=2;i<=num;i++);
sum=sum+i;
printf("1\%d",num);
printf("\nsum=1/%d",sum+1/num);
return0;
}
